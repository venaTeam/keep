---
title: "Ticket Count"
sidebarTitle: "Ticket Count Provider"
description: "Ticket Count provider exposes a lightweight HTTP endpoint that Keep can query to display ServiceNow ticket statistics on dashboards."
---
import AutoGeneratedSnippet from '/snippets/providers/ticket-count-snippet-autogenerated.mdx';

<AutoGeneratedSnippet />

## Connecting with the Provider

1. Deploy or expose an HTTP endpoint that returns ticket counts in the expected JSON format:
   - `{"count": <number>}` – standard response
   - `{"Team not found": 0}` – optional response when the requested team is missing
2. Configure the provider in Keep and supply the `count_url` (must be reachable from the Keep backend).
3. (Optional) The widget can append query parameters (`team`, `state`, `detection`) – ensure your endpoint can parse and handle them.

## Notes

- The provider is intended for read-only dashboards; it does not expose workflow actions.
- The URL can be `http://` or `https://`, including localhost/IP addresses for development environments.
- Make sure the endpoint responds quickly; the widget polls it periodically.

## Useful Links

_No information yet, feel free to contribute it using the "Edit this page" link at the bottom of the page_


